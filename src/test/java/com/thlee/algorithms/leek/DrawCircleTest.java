package com.thlee.algorithms.leek;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class DrawCircleTest {

    @Test
    void drawCircle() {
        DrawCircle drawCircle = new DrawCircle();
        drawCircle.drawCircle(0, 0, 20);
    }
}